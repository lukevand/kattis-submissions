#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(false);
    int N, n, x;
    string cmd;
    string line;
    cin >> N;
    while (N--) {
        vector<int> v;
        cin >> cmd;
        cin >> n;
        cout << n;
        getline(cin, line);
    }
    return 0;
}

